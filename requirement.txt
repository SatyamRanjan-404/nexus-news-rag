# requirements.txt

# ── Force CPU-only PyTorch FIRST (must be before sentence-transformers)
# This tells pip: use the lightweight CPU build, not the 532MB CUDA build
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
torchvision==0.17.2+cpu

# ── Now sentence-transformers will use the CPU torch above
sentence-transformers==2.7.0

# ── Everything else unchanged
feedparser==6.0.11
langchain==0.1.16
langchain-community==0.0.34
langchain-groq==0.1.3
langchain-core==0.1.53
chromadb==0.5.0
streamlit==1.33.0
python-dotenv==1.0.1
requests==2.31.0
apscheduler==3.10.4